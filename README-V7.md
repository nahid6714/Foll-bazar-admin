# Fol Bazar Admin Desktop v7

- Admin session re-validation on startup
- Current admin account status
- PHP/MySQL connection test
- Database health check
- Site Settings CRUD
- Generic customer editor cannot change `profiles.role`
- Existing product/variant/stock/order/customer/complaint/coupon/banner/wishlist workflows retained

Build on Windows: `npm install` then `npm run dist`.
Never place PHP/MySQL service-role keys or server storage API secrets in the desktop app.
